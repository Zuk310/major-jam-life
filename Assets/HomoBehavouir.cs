using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HomoBehavouir : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //HOMOERRECTION 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
